/**
 * Simulated function to get user email from userId
 * Replace this with a real DB or API call later.
 * 
 * @param {string} userId
 * @returns {Promise<string>}
 */
const getUserEmail = async (userId) => {
  // Replace with actual user lookup
  return "aom.k@ahduni.edu.in";
};

module.exports = getUserEmail;
